name: Update Bhopal Weather Card

on:
  schedule:
    - cron: "0 */3 * * *"   # Runs every 3 hours
  workflow_dispatch:        # Allows manual trigger
  push:
    branches:
      - main
    paths:
      - 'scripts/update_weather.py'
      - '.github/workflows/weather.yml'

jobs:
  update-weather:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Run Weather Script
        run: python scripts/update_weather.py

      - name: Commit and Push SVG
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/weather/bhopal.svg
          git diff-index --quiet HEAD || git commit -m "chore: auto-update Bhopal weather card [skip ci]"
          git push origin main
